from collections import OrderedDict
import math
with open('star5.csv','r') as f:
	lines = f.read().split('\n') # splitting each line
points = []
i=0
for line in lines:
	if i!=0: points.append(line.split(','))
	i+=1
final_data = OrderedDict() # final_data is Wavelength: Flux
for i in range(1,len(points)-1):
	final_data[float(points[i][0])] = float(points[i][1])